import { DigTemplateDirective } from './dig-template.directive';

describe('DigTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new DigTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
