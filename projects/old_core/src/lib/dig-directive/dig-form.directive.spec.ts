import { DigFormDirective } from './dig-form.directive';

describe('DigFormDirective', () => {
  it('should create an instance', () => {
    const directive = new DigFormDirective();
    expect(directive).toBeTruthy();
  });
});
