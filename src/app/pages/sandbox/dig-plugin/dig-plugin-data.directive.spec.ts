import { DigPluginDataDirective } from './dig-plugin-data.directive';

describe('DigPluginDataDirective', () => {
  it('should create an instance', () => {
    const directive = new DigPluginDataDirective();
    expect(directive).toBeTruthy();
  });
});
