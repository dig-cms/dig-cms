import { DigFormGroupDirective } from './dig-form-group.directive';

describe('DigFormGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new DigFormGroupDirective();
    expect(directive).toBeTruthy();
  });
});
