import { DigDocDirective } from './dig-doc.directive';

describe('DigDocDirective', () => {
  it('should create an instance', () => {
    const directive = new DigDocDirective();
    expect(directive).toBeTruthy();
  });
});
