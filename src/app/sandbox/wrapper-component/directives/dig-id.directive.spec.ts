import { DigIdDirective } from './dig-id.directive';

describe('DigIdDirective', () => {
  it('should create an instance', () => {
    const directive = new DigIdDirective();
    expect(directive).toBeTruthy();
  });
});
